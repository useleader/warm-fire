---
publish: true
---

1. `&`
	- 任意一条指令放在背景中运行，添加这个符号，表示后台运行，可以继续输入指令
	- eg: `ping baidu.com & apt-get update &`
2. `;`
	- 指令按顺序执行
3. `&&`
	- 逻辑上的与，前面指令顺利执行了才会执行后面的指令
4. `||`
	- 逻辑上的或，前面指令顺利执行了就不会再执行后面的指令，前失败才会后运行
5. `|`
	- 前者的输出作为后者的输入