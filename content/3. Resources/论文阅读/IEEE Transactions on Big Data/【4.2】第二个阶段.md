---
publish: true
---

UR方面同第一个阶段，感觉.o文件编译到一起后应该是能运行的吧，先试试
成功编译了，希望效果能好用
```
cc -O3 -funroll-loops -Wall -D_FORTIFY_SOURCE=2 -g -Wno-pointer-sign -Wno-unused-result -DAFL_PATH="/usr/local/lib/afl" -DDOC_PATH="/usr/local/share/doc/afl" -DBIN_PATH="/usr/local/bin" -c evofuzz_trace.c -o evofuzz_trace.o

cc -O3 -funroll-loops -Wall -D_FORTIFY_SOURCE=2 -g -Wno-pointer-sign -Wno-unused-result -DAFL_PATH="/usr/local/lib/afl" -DDOC_PATH="/usr/local/share/doc/afl" -DBIN_PATH="/usr/local/bin" -c evofuzz_semantic.c -o evofuzz_semantic.o

cc -O3 -funroll-loops -Wall -D_FORTIFY_SOURCE=2 -g -Wno-pointer-sign -Wno-unused-result -DAFL_PATH="/usr/local/lib/afl" -DDOC_PATH="/usr/local/share/doc/afl" -DBIN_PATH="/usr/local/bin" -c evofuzz_llm.c -o evofuzz_llm.o

cc -O3 -funroll-loops -Wall -D_FORTIFY_SOURCE=2 -g -Wno-pointer-sign -Wno-unused-result -DAFL_PATH="/usr/local/lib/afl" -DDOC_PATH="/usr/local/share/doc/afl" -DBIN_PATH="/usr/local/bin" -c evofuzz_mutation.c -o evofuzz_mutation.o

cc -O3 -funroll-loops -Wall -D_FORTIFY_SOURCE=2 -g -Wno-pointer-sign -Wno-unused-result -DAFL_PATH="/usr/local/lib/afl" -DDOC_PATH="/usr/local/share/doc/afl" -DBIN_PATH="/usr/local/bin" -c evofuzz_compat.c -o evofuzz_compat.o
```


```
docker build -t evo-phase2 -f tutorials/live555/Dockerfile .
```

此时已经部署编译好并保存在evofuzz-live555中，并想用wireshark监听下情况
```
docker run -it --network host -v $(pwd)/workdir:/home/root/workdir evo-phase2 /bin/bash

docker run -it --privileged=true --name your_container_name your_image_name
# --privileged=true为将容器的权限提升，进行echo core >/proc/sys/kernel/core_pattern

docker run -it --privileged=true --network host -v $(pwd)/workdir:/home/root/workdir evo-phase2 /bin/bash
```

```
export EVOFUZZ_API_KEY=sk-Hc6IGVxPkGHUw6tp3c9b78574f2d41C3Aa69EeD974893bD2
```

```
afl-fuzz -d -i $AFLNET/tutorials/live555/in-rtsp -o out-live555 -N tcp://127.0.0.1/8554 -x $AFLNET/tutorials/live555/rtsp.dict -P RTSP -D 10000 -q 3 -s 3 -E -K -R ./testOnDemandRTSPServer 8554

```

接着进行调试即可，目前还是比较顺利的


编译的相关配置在.patch文件里面

找到了一个可能的方向，由于evo-fuzz并没有部署导致的

在llvm_mode安装部分遇到了麻烦，环境问题真是烦人

目前在镜像中已能编译成功，此时遇到了
[-]  SYSTEM ERROR : No server states have been detected. Server responses are likely empty!
    Stop location : main(), afl-fuzz.c:9485
       OS message : Interrupted system call
明日解决，确定了是运行in-rtsp的问题

去寻找日志的记录：输出在out-live555文件夹内

部署效果：
梳理一下