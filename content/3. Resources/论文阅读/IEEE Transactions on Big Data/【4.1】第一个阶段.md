---
publish: true
---


```
docker build -t evo-fuzz -f tutorials/live555/Dockerfile .
```

此时已经部署编译好并保存在evofuzz-live555中，并想用wireshark监听下情况
```
docker run -it --network host -v $(pwd)/workdir:/home/root/workdir evo-fuzz /bin/bash
```

```
afl-fuzz -d -i $AFLNET/tutorials/live555/in-rtsp -o out-live555 -N tcp://127.0.0.1/8554 -x $AFLNET/tutorials/live555/rtsp.dict -P RTSP -D 10000 -q 3 -s 3 -E -K -R ./testOnDemandRTSPServer 8554

```



编译的相关配置在.patch文件里面

找到了一个可能的方向，由于evo-fuzz并没有部署导致的

在llvm_mode安装部分遇到了麻烦，环境问题真是烦人

目前在镜像中已能编译成功，此时遇到了[-]  SYSTEM ERROR : No server states have been detected. Server responses are likely empty!
    Stop location : main(), afl-fuzz.c:9485
       OS message : Interrupted system call
明日解决，确定了是运行in-rtsp的问题

去寻找日志的记录：输出在out-live555文件夹内

## 部署效果
实现轨迹收集
func_call, func_ret, memory_read, memory_write, branch_taken

根据不同协议指定特定的收集方式


### 初始化阶段
程序启动 → 加载配置 → 初始化语义分析 → 创建增强状态队列


读取种子文件 → 提取请求区域 → 建立状态序列 → 进行初始语义分析

在send_over_network中先start_trace_collection(); // 开始记录执行轨迹
